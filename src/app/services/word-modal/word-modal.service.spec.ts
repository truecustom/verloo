import { WordModalService } from './word-modal.service';

describe('WordModalService', () => {
  const service: WordModalService = new WordModalService();

  it('should create', () => {
    expect(service).toBeTruthy();
  });
});
