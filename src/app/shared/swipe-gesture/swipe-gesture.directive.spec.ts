import { SwipeGestureDirective } from './swipe-gesture.directive';

describe('SwipeGestureDirective', () => {
  let directive: SwipeGestureDirective;

  beforeEach(() => {
    directive = new SwipeGestureDirective();
  });

  it('should create', () => {
    expect(directive).toBeTruthy();
  });
});
