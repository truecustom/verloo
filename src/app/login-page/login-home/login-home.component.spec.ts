import { LoginHomeComponent } from './login-home.component';

describe('LoginHomeComponent', () => {
  let component: LoginHomeComponent;

  beforeEach(() => {
    component = new LoginHomeComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
