import { HelpHomeComponent } from './help-home.component';

describe('HelpHomeComponent', () => {
  let component: HelpHomeComponent;

  beforeEach(() => {
    component = new HelpHomeComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
