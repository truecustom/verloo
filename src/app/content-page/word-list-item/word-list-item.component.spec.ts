import { WordListItemComponent } from './word-list-item.component';

describe('WordListItemComponent', () => {
  let component: WordListItemComponent;

  beforeEach(() => {
    component = new WordListItemComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
