import { WordDialogComponent } from './word-dialog.component';

describe('WordDialogComponent', () => {
  let component: WordDialogComponent;

  beforeEach(() => {
    component = new WordDialogComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
