import { ContentListComponent } from './content-list.component';

describe('ContentListComponent', () => {
  let component: ContentListComponent;

  beforeEach(() => {
    component = new ContentListComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
