import { ContentHomeComponent } from './content-home.component';

describe('ContentHomeComponent', () => {
  let component: ContentHomeComponent;

  beforeEach(() => {
    component = new ContentHomeComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
